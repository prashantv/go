## Go Monorepo of Modules

This is a Go monorepo which is made up many small low-dependency packages.

This allows browsing the collection of packages as a whole,
while reducing development costs of these packages.

The packages are intentionally written so they can be easily copy+pasted
and modified as required for other projects.
